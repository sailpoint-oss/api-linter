{
  "name": "api-linter",
  "version": "1.0.0",
  "description": "SailPoint Maintained API Linter toolchain",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "run": "pnpm -r run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.5.0",
    "typescript": "^4.9.5",
    "vitest": "^0.34.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
